name: Build and Deploy

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Build with Jekyll
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./
          destination: ./_site
      - name: Upload artifact for github-pages native
        uses: actions/upload-pages-artifact@v3

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      ## GitHub Pages Mirror
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
      ## debug print
      - run: |
          find . -type f
      ## AWS s3
      - uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-region: 'ap-northeast-1'
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
      - name: Downlaod website files artifact
        uses: actions/download-artifact@v5
        with:
          name: github-pages
      - name: Deploy to AWS S3
        run: |
          aws s3 sync . s3://ndxbn.tokyo/ --delete
  # deploy-to-s3:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: aws-actions/configure-aws-credentials@v1
  #       with:
  #         aws-region: 'ap-northeast-1'
  #         role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
  #     - name: Downlaod website files artifact
  #       uses: actions/download-artifact@v5
  #       with:
  #         name: github-pages
  #     - name: Deploy to AWS S3
  #       run: |
  #         aws s3 sync . s3://ndxbn.tokyo/ --delete
